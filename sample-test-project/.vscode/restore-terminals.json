{
  "runOnStartup": true,
  "keepExistingTerminalsOpen": false,
  "terminals": [
    {
      "cwd": "${workspaceFolder}",
      "splitTerminals": [
        {
          "name": "server",
          "icon": "server-process",
          "color": "terminal.ansiGreen",
          "commands": [
            "echo 'window 1, split 1 of 3 — expect a green server icon'",
            "echo 'two commands ran in this terminal, this is the second'"
          ]
        },
        {
          "name": "client",
          "icon": "browser",
          "color": "terminal.ansiBlue",
          "commands": ["echo 'window 1, split 2 of 3 — expect a blue browser'"]
        },
        {
          "name": "cwd override",
          "icon": "folder",
          "color": "terminal.ansiMagenta",
          "cwd": "${workspaceFolder}/.vscode",
          "commands": [
            "echo 'window 1, split 3 of 3 — this one should be in .vscode, not the workspace root:'",
            "pwd"
          ]
        }
      ]
    },
    {
      "splitTerminals": [
        {
          "name": "paste only",
          "icon": "tools",
          "color": "terminal.ansiYellow",
          "shouldRunCommands": false,
          "commands": [
            "echo 'window 2, split 1 of 2 — this text should sit unexecuted at the prompt'"
          ]
        },
        {
          "name": "env",
          "icon": "gear",
          "color": "terminal.ansiCyan",
          "env": { "RESTORE_TERMINALS_SAMPLE": "env-var-works" },
          "commands": [
            "echo \"window 2, split 2 of 2 — expect env-var-works: $RESTORE_TERMINALS_SAMPLE\""
          ]
        }
      ]
    },
    {
      "location": "editor",
      "splitTerminals": [
        {
          "name": "editor area",
          "icon": "book",
          "color": "terminal.ansiRed",
          "commands": [
            "echo 'window 3, split 1 of 2 — this window should be an editor tab, not in the panel'"
          ]
        },
        {
          "name": "editor split",
          "icon": "book",
          "color": "terminal.ansiRed",
          "commands": [
            "echo 'window 3, split 2 of 2 — split beside the other editor terminal'"
          ]
        }
      ]
    }
  ]
}
